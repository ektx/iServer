module.exports = function SocketIO (io) {
	io.on('connection', function (socket) {
		// 暂时无功能需求，备用
		// https://socket.io/get-started/chat/
		// console.log('CONN IO - A user connected')
		// watch(socket)
	})
}